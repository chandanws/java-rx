/**
 * <h3>RxJava (Reactive Java) - A library for composing asynchronous and event-based programs.</h3>
 * <br>
 * Reactive means that we <b>react</b> to some changes, whenever those changes happen, rather than trying to <b>request
 * and wait</b> until when those changes actually happen.
 * <br>
 */
package rx.knowledge.sharing;